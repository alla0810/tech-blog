<div class="col-md-12">
  <div class="card">
    <div class = "card-header display-flex justify-space-between">
      <div class = "font-title">{{title}}</div>
      <div class = "font-date">Posted by {{user.name}} on {{format_date date_created}}</div>
    </div>

  <p class = "card-body">{{description}}</p>
  </div>
</div>

<br>

<div class="col-md-12">
    <div class="card" >
      <h4 class = "card-header"> &nbsp </h4>

      <form class="form login-form">
        <div class="form-group">
          <label for="password-login">Comment</label>
          <textarea class="form-input" id="Blog-Comment" rows="4"> </textarea>
        </div>
        <div class="form-group">
          <button class="btn" type="submit">Submit</button>
        </div>
      </form>

    </div>
  </div>


<script src="./js/commentpost.js"></script>